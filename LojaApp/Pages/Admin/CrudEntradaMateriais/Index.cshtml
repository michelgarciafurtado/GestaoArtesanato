@page
@model LojaApp.Pages.CrudEntradaMateriais.IndexModel

@{
    ViewData["Title"] = "Lista de entrada de materias";
}
@{
        if (TempData["Mensagem"] != null)
    {
        <div class="alert alert-success">
            @TempData["Mensagem"]
        </div>
    }
}

<h1 class="mb-3">Gerenciamento de Entrada de Materiais</h1>
<hr />
<p>
    <a asp-page="Create" class="btn btn-success">Adicionar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EntradaMaterial[0].DataEntrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntradaMaterial[0].MateriaPrima)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntradaMaterial[0].Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntradaMaterial[0].PesoUn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntradaMaterial[0].TipoMedida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntradaMaterial[0].ValorUn)
            </th>
            <th>Valor Total</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EntradaMaterial) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntrada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MateriaPrima.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PesoUn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoMedida)
            </td>
            <td>
               R$ @Html.DisplayFor(modelItem => item.ValorUn)
            </td>
            <td>
                @item.ValorTotal
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntrada" class="btn btn-light">Editar</a> |
                    <a asp-page="./Details" asp-route-id="@item.IdEntrada" class="btn btn-info">Detalhes</a> |
                    <!-- Excluir -->
                    <form method="post" class="d-inline">
                        <button type="submit"
                                asp-page-handler="DeleteEntrada"
                                asp-route-IdEntrada="@item.IdEntrada"
                                class="btn btn-sm btn-danger"
                                title="Excluir">
                                Excluir
                        </button>
                    </form>
            </td>
        </tr>
}
    </tbody>
</table>
